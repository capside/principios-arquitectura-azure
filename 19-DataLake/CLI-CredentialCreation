Command on Azure CLI 2.0 to create a credential on DLA so it can use an Azure SQL DB as a Datasource

az dla catalog credential create 
--account ??dlaAccountName 
--credential-name ??nameOfCredential 
--database-name ??nameOfDB-InDLA 
--uri tcp://??servername.database.windows.net 
--user-name ??userName
